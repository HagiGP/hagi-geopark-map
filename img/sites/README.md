# サイト写真フォルダ

各見どころ（サイト）の写真をここに置くと、地図のポップアップに表示されます。

- ファイル名の規則：`<サイト名>.jpg`（例：`菊ヶ浜.jpg`）
  - サイト名に `\ / : * ? " < > |` が含まれる場合は `_` に置き換え
- 横800px程度・JPEGを推奨（軽量化のため）
- 運営モード（ロゴを3秒長押し）→ 地点のポップアップ →「📷 写真」から、
  ドラッグ＆選択で自動的に軽量化・命名して保存できます
- 写真が無いサイトは、ポップアップに画像を出しません（自動判定）
